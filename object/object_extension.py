# ！/usr/bin/python

class ObjectExtension:
    def contains_attr(self, o: object, path: str) -> bool:
        if path is not None and o is not None:
            pass
        else:
            return
